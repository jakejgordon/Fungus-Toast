# === General Text Normalization ===
# Keep the repo canonical LF; override specific Windows-only types below.
*               text=auto eol=lf

# === Unity ===
# Meta files (LF avoids Unity warnings)
*.meta          text eol=lf

# Unity YAML-based assets (force LF for clean diffs).
# merge=unityyamlmerge => semantic 3-way merge via Unity's SmartMerge tool.
# Activate the driver locally once with: pwsh tools/git/setup.ps1  (or bash tools/git/setup.sh)
*.unity         text eol=lf merge=unityyamlmerge
*.prefab        text eol=lf merge=unityyamlmerge
*.asset         text eol=lf merge=unityyamlmerge
*.mat           text eol=lf merge=unityyamlmerge
*.anim          text eol=lf merge=unityyamlmerge
*.controller    text eol=lf merge=unityyamlmerge
*.overrideController text eol=lf merge=unityyamlmerge
*.shader        text eol=lf
*.cginc         text eol=lf
*.hlsl          text eol=lf
*.shadergraph   text eol=lf
*.txt           text eol=lf
*.json          text eol=lf

# C# / .NET / project files
*.cs            text eol=lf
*.csproj        text eol=lf
*.props         text eol=lf
*.targets       text eol=lf
*.xml           text eol=lf
*.md            text eol=lf

# Solution file — pick ONE policy for consistency.
# Keep this (LF) OR switch to eol=crlf if VS keeps rewriting it.
*.sln           text eol=lf

# Scripts
*.sh            text eol=lf
*.bat           text eol=crlf
*.cmd           text eol=crlf

# === Binary / Non-diffable Assets ===
*.png           binary
*.jpg           binary
*.jpeg          binary
*.psd           binary
*.mp3           binary
*.wav           binary
*.ogg           binary
*.dll           binary
*.pdb           binary
*.ttf           binary
*.otf           binary
*.fbx           binary
*.animset       binary
*.unitypackage  binary
*.zip           binary
*.7z            binary
*.mp4           binary
*.mov           binary
